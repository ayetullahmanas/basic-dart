// ignore_for_file: unused_local_variable

void main() {
  /// (Değişken Türü) (Değişken Adı) = (Değeri)  şeklinde olur

  /// Not: Değişken adı önüne alt tire ( _ ) işareti konulursa,
  /// o değişkene sadece kullanıldığı sınıf içinde erişim sağlanabilir.
  /// Buna ['private değşiken'] adı veririlir.

  int sayi = 2; // public değişken

  String _isim = "ahmet"; // private değişken

  int ilkRakam = 5; // Camel Case değişken tanımlama

  String ad_soyad = "Ali Yıldız"; // Snake Case değişken tanımlama

  String _private_snake_case = "private değişken ve snake case tanımlama";
}
